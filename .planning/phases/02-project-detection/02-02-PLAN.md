---
phase: "02-project-detection"
plan: "02"
type: "execute"
wave: 2
depends_on: ["02-01-PLAN.md"]
files_modified:
  - "packages/ui-core/app.js"
  - "packages/ui-core/index.html"
  - "packages/ui-core/style.css"
autonomous: true
requirements:
  - "SRC-03"
  - "UI-01"
  - "OBS-01"
  - "INT-03"
must_haves:
  truths:
    - "Each registered source in the sidebar shows text method badges (GSD, BMAD, Spec-Kit) in ROADMAP order when returned by the API"
    - "Main column shows a Current Focus block with phase, plan, status, progress and Refresh status; polling calls GET /api/source/overview?sourceId= on a 2–5s tick when tab is visible"
  artifacts:
    - path: "packages/ui-core/app.js"
      provides: "Polling, overview fetch, method badges, Current Focus card"
  key_links:
    - from: "packages/ui-core/app.js"
      to: "GET /api/source/overview?sourceId"
      via: "fetch in poll + manual refresh"
---

<objective>
Layer **Source sidebar** and **Current Focus** dashboard UI on the Phase 1 shell: method **badges**, **unified status** (OBS-01 / UI-01), **INT-03** polling + manual refresh, all aligned with `02-UI-SPEC.md` and tokens in existing `style.css` / `index.html`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-project-detection/02-UI-SPEC.md
@.planning/phases/02-project-detection/02-CONTEXT.md
@packages/ui-core/app.js
@packages/ui-core/index.html
@packages/ui-core/style.css
</context>

<tasks>

<task type="execute">
  <name>API_BASE-safe overview fetcher</name>
  <read_first>
    - `packages/ui-core/app.js` (entire file)
  </read_first>
  <action>
    Add `const API_BASE` defaulting to ` window.location.protocol === "file:" ? "http://127.0.0.1:3000" : ""` and prefix all fetches: `` `${API_BASE}/api/...` `` (registry already uses root-relative; replace `fetch('/api/registry')` with `fetch(\`\${API_BASE}/api/registry\`)` and the new overview fetches the same way). This preserves file:// dev per ui-gsd pattern. Add async `fetchSourceOverview(id)` that GETs `api/source/overview?sourceId=${encodeURIComponent(id)}` and returns parsed JSON; on `!res.ok` return `{ error: true, status: res.status }`. Store in `state` as `state.overviewById: Record id -> object | null` and `state.lastUpdatedAt: number` ms epoch for "Updated" label. Do not use any bundler; vanilla JS only.
  </action>
  <acceptance_criteria>
    - `grep -q "API_BASE" packages/ui-core/app.js`
    - `grep -q "source/overview" packages/ui-core/app.js`
    - `grep -F "api/registry" packages/ui-core/app.js | head -1` matches pattern containing `API_BASE`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Method badges in sidebar + active source</name>
  <read_first>
    - `packages/ui-core/app.js` (renderSidebar)
    - `.planning/phases/02-project-detection/02-UI-SPEC.md` (Spacing + Color for badges)
  </read_first>
  <action>
    After `fetchSources`, for each `source` call `fetchSourceOverview(source.id)` once (or batch in future) and save `state.overviewById[source.id]`. In `renderSidebar`, each `li.sidebar-item` must include: folder icon, `source.name`, and a `div.method-badges` with `span.method-badge` for each string in `overview.methods` from API; if `overview` missing, show a muted “…” or skip badges until load completes. Order badges as returned by the API. Apply CSS: `.method-badge` font-size 12px, padding 4px 8px, border 1px solid `var(--border, #27272a)`, border-radius 9999px, margin-left 4px, color `var(--text-dim, #94a3b8)`; active row uses `var(--primary, #3b82f6)` border for selected source only. Do not add icon fonts. Match UI-SPEC “text badges”. Lucide `folder` icon can remain. Click handler unchanged: sets `state.currentSource` to `data-id` string.
  </action>
  <acceptance_criteria>
    - `grep -q "method-badge" packages/ui-core/index.html` OR `grep -q "method-badge" packages/ui-core/app.js` (string in template)
    - `grep -q "method-badges" packages/ui-core/style.css` OR in `app.js` inline template includes `class="method-badge"`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Current Focus + empty/error copy (UI-SPEC)</name>
  <read_first>
    - `.planning/phases/02-project-detection/02-UI-SPEC.md` (Copywriting Contract + hierarchy)
  </read_first>
  <action>
    Replace the placeholder `project-dashboard` block in `renderMainArea` when a source is selected. Render: **Heading** “Current status” (20px/600) not larger than spec heading role; a card `div.current-focus` with: line 1 **Phase** value from `state.overviewById[selected].state.focus.phase` or "—" if empty; line 2 **Plan**; line 3 **Status**; line 4 **Progress** (plain text or monospace per spec: progress line may be plaintext). If `overview.state` is null, or has `error === "missing"`: show **Heading** “No status file yet” and **Body** text exactly: `This folder has no .planning/STATE.md. Initialize your SDD workflow, then return here or use Refresh status.` If `fetchSourceOverview` returned `error: true` with 404/500: show **Heading** “Couldn’t load status” and **Body** `Check that the SDD UI server is running, then use Refresh status.` (HTTP status in small dim text if error object has `status`.) **Primary CTA** button with visible text `Refresh status` that calls `fetchSourceOverview` + `render()`; place below card or in header row. Muted `p.last-updated` with text `Updated Xs ago` from `state.lastUpdatedAt` vs `Date.now()`.
  </action>
  <acceptance_criteria>
    - `grep -q "Refresh status" packages/ui-core/app.js`
    - `grep -q "No status file yet" packages/ui-core/app.js` OR in template literal inside app.js
    - `grep -q "current-focus" packages/ui-core/app.js` OR `style.css`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>INT-03 polling and visibility</name>
  <read_first>
    - `packages/ui-core/app.js`
  </read_first>
  <action>
    When `state.currentSource` is set, start a `setInterval` every **3000** ms to call `fetchSourceOverview` + `render()`; clear interval when `currentSource` null or on `beforeunload`. On `document.visibilitychange`, if `document.visibilityState === "hidden"`, do not start new fetches; when visible again, immediately fetch once and restart interval. Store interval id on `state.pollId` to avoid duplicates. Optional: if JSON includes `content_sha256` equal to previous, skip re-render of main innerHTML to reduce flicker (compare before DOM write).
  </action>
  <acceptance_criteria>
    - `grep -E "setInterval\([^,]+,\s*3000" packages/ui-core/app.js` returns match
    - `grep -q "visibilitychange" packages/ui-core/app.js`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Style polish for new components</name>
  <read_first>
    - `packages/ui-core/style.css`
  </read_first>
  <action>
    Add BEM-style classes: `.current-focus` card uses padding 16px (md), border 1px solid border token, border-radius 16px, background `var(--card-bg or #111827)` to match `dark-theme` shell; do not set font-size to more than 4 distinct sizes in new rules (per UI-SPEC: label 12px, body 16px, heading 20px). Add `.current-focus` subtle transition `opacity` 0.15s to support optional focus-card pulse on refresh (add class for 200ms on Refresh click). **Do not** set main `padding: 40px` on new elements—use 32px/24px (xl/lg) for new areas only, leave legacy as-is. Ensure `Refresh status` is `btn-primary` or `btn-secondary` consistent with `style.css` button classes.
  </action>
  <acceptance_criteria>
    - `grep -q ".current-focus" packages/ui-core/style.css`
    - `grep -E "1[6-9]px|20px" packages/ui-core/style.css | head -5` (at least one rule for new layout uses spec spacing scale 16/20/24)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | `sourceId` is opaque to UI; all reads scoped server-side to registry (inherits Plan 1) |

## STRIDE register

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-02-03 | DoS (polling) | accept | 3s interval, pause when tab hidden; local tool |

**Block on high:** XSS from STATE body — `innerHTML` must use **textContent** for dynamic STATE excerpts or `escapeHtml` helper; **forbidden** to inject raw `state.state.body` into innerHTML. Executor must set text nodes for focus fields only, not unescaped markdown.
</threat_model>

<verification>
1. `rg "innerHTML" packages/ui-core/app.js` and confirm Current Focus does not assign raw `body` with HTML from file.
2. Run server + browser: add this repo, select source, confirm badges, Current Focus, polling (network tab 3s), Refresh, hidden tab throttling.
</verification>

<success_criteria>
- UI matches UI-SPEC copy for empty and error paths.
- Polling 3s, visibility pause, Refresh button, Updated label.
- Method badges and active row styling do not drown the layout in primary accent.
</success_criteria>

<output>
After execution, create `.planning/phases/02-project-detection/02-02-SUMMARY.md`.
</output>

## PLANNING COMPLETE

(Plans ready for gsd-execute-phase — wave 2)
