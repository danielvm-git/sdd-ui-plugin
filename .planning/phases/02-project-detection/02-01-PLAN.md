---
phase: "02-project-detection"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "packages/ui-core/server/registry.py"
  - "packages/ui-core/server/detection.py"
  - "packages/ui-core/server/state_focus.py"
  - "packages/ui-core/server/handlers.py"
  - "packages/ui-core/server/router.py"
autonomous: true
requirements:
  - "SRC-04"
  - "OBS-01"
  - "INT-03"
must_haves:
  truths:
    - "Python resolves a registry id to an absolute on-disk project root and never uses a path string from the query string to choose arbitrary files"
    - "GET /api/source/overview?sourceId=<id> returns JSON with methods and state payload suitable for the dashboard"
  artifacts:
    - path: "packages/ui-core/server/detection.py"
      provides: "Filesystem marker table for GSD, BMAD, Spec-Kit"
    - path: "packages/ui-core/server/state_focus.py"
      provides: "STATE.md current-focus extraction"
  key_links:
    - from: "packages/ui-core/server/router.py"
      to: "packages/ui-core/server/handlers.py"
      via: "GET /api/source/overview"
---

<objective>
Ship stdlib Python **per-source** SDD method **detection** and **.planning/STATE.md** “Current Focus” parsing behind one registry-scoped **GET** API, so the unified dashboard (Phase 1 shell) can render workflow status without shelling to Node.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-project-detection/02-CONTEXT.md
@.planning/phases/02-project-detection/02-RESEARCH.md
@.planning/phases/02-project-detection/02-UI-SPEC.md
@packages/ui-core/server/registry.py
@packages/ui-core/server/router.py
@packages/ui-core/server/handlers.py
</context>

<tasks>

<task type="execute">
  <name>Registry lookup by id</name>
  <read_first>
    - `packages/ui-core/server/registry.py`
    - `packages/ui-core/server/handlers.py`
  </read_first>
  <action>
    Add `get_by_id(self, source_id: str)` to `RegistryManager` that returns the dict for matching `id` or `None`. Document that `id` is stringified timestamp from Phase 1. Do not change the on-disk JSON schema.
  </action>
  <acceptance_criteria>
    - `packages/ui-core/server/registry.py` contains the exact substring `def get_by_id`
    - `get_by_id` returns `None` when no item matches (grep for `return None` after loop)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Method detection module</name>
  <read_first>
    - `packages/adapters/src/gsd.js` (for intent only, no imports)
    - `packages/ui-core/server/detection.py` (new file to create)
  </read_first>
  <action>
    Create `packages/ui-core/server/detection.py` with a module docstring that contains a **Marker table (v1)** with exactly these substrings: `.planning/ROADMAP.md`, `.bmad/`, `.specify/`. Implement `detect_methods(project_root: str) -> list[str]` returning a list ordered **GSD → BMAD → Spec-Kit** with only values `"gsd"`, `"bmad"`, `"spec-kit"` and no duplicates. GSD: true if `os.path.isdir(join(root,".planning"))` and exists `join(root,".planning","ROADMAP.md")` or `join(root,".planning","STATE.md")`. BMAD: `join(root,".bmad")` is dir. Spec-Kit: `join(root,".specify")` is dir. Use `os.path` only, utf-8 not required for path checks. If `project_root` missing or not a directory, return `[]`.
  </action>
  <acceptance_criteria>
    - `python3 -m compileall -q packages/ui-core/server` exits 0
    - `grep -q "def detect_methods" packages/ui-core/server/detection.py`
    - `grep -q "Marker table" packages/ui-core/server/detection.py`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>STATE.md focus extraction</name>
  <read_first>
    - `.planning/STATE.md` (example shape in this repo)
    - `packages/ui-gsd/app.py` (frontmatter `get_frontmatter` pattern)
    - new `packages/ui-core/server/state_focus.py`
  </read_first>
  <action>
    Create `packages/ui-core/server/state_focus.py` with `parse_state(path: str) -> dict` that: if file missing returns `{"error": "missing"}`; else read utf-8, split `---` frontmatter like `get_frontmatter` in ui-gsd, build dict with `metadata` (key/values from frontmatter lines), `body`, and `focus` object with string keys `phase`, `plan`, `status`, `progress` (empty string if not found) by scanning **body** for lines containing `**Phase**`, `**Plan**`, `**Status**`, `**Progress**` and capturing text after the first `**` pair on the same line. Include `content_sha256` = sha256 of full file (hex) from `hashlib` stdlib. No third-party packages.
  </action>
  <acceptance_criteria>
    - `python3 -m compileall -q packages/ui-core/server` exits 0
    - `grep -q "content_sha256" packages/ui-core/server/state_focus.py`
    - `grep -q "def parse_state" packages/ui-core/server/state_focus.py`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>GET /api/source/overview</name>
  <read_first>
    - `packages/ui-core/server/router.py`
    - `packages/ui-core/server/handlers.py`
    - `packages/ui-core/server/detection.py`
    - `packages/ui-core/server/state_focus.py`
  </read_first>
  <action>
    Register `GET /api/source/overview` in `Router.routes` mapping to a new `Handlers.get_source_overview` static method. Parse query string: require `sourceId`. Load registry, `get_by_id(sourceId)`; if missing, respond `404` JSON `{"error":"Unknown source"}`; if `path` not `os.path.isdir`, respond `400` JSON `{"error":"Path not found"}`. Otherwise call `detect_methods(absolute_path)` and map to display labels: `GSD` if `"gsd"` in list, `BMAD` if `"bmad"`, `Spec-Kit` if `"spec-kit"` in order. Build `methods: string[]` in that GSD–BMAD–Spec-Kit order. Parse `join(absolute,".planning","STATE.md")` with `parse_state`. Response `200` JSON: `id`, `path`, `methods` (string labels for UI), `raw_methods` (internal short names), `state` (output of `parse_state` or `null` if missing), `stateMtime` (float from `os.path.getmtime` if file exists else null). Reuse `Handlers` JSON helper pattern: `send_response`, headers, `json.dumps` utf-8. Import from `urllib.parse` `urlparse` + `parse_qs` to read `handler.path` including query. No extra routes required.
  </action>
  <acceptance_criteria>
    - `grep -q "get_source_overview" packages/ui-core/server/handlers.py`
    - `grep -q "/api/source/overview" packages/ui-core/server/router.py`
    - `grep -q "sourceId" packages/ui-core/server/handlers.py`
    - `grep -q "404" packages/ui-core/server/handlers.py` (unknown source branch) OR `grep -q "Unknown" packages/ui-core/server/handlers.py`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `GET /api/source/overview` | `sourceId` is untrusted; must resolve only through registry; never `open()` on user-supplied path from query for arbitrary reads |

## STRIDE register

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-02-01 | Elevation of privilege / path traversal | mitigate | Resolve path only from registry id; ignore any client-supplied directory path in GET |
| T-02-02 | Information disclosure (registry) | accept | same trust model as GET /api/registry; local dev tool |

**Block on high:** path traversal; mitigated by registry-only resolution.
</threat_model>

<verification>
1. `python3 -m compileall -q packages/ui-core/server`
2. Start `python3 packages/ui-core/app.py`, obtain a `sourceId` from `GET /api/registry` after registering this repo, then `GET /api/source/overview?sourceId=...` and confirm `methods` is a JSON array and `state` is object or `null` key present.
</verification>

<success_criteria>
- Overview endpoint is callable and returns stable JSON for real registry entries.
- Detection returns ordered method labels and never reads outside the registered root.
- STATE parsing returns sha256 and focus fields for `.planning/STATE.md` in this monorepo.
</success_criteria>

<output>
After execution, create `.planning/phases/02-project-detection/02-01-SUMMARY.md` with what landed and any marker-table tweaks.
</output>

## PLANNING COMPLETE

(Plans ready for gsd-execute-phase — wave 1)
