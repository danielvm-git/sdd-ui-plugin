---
phase: "03-roadmap-visualization"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "packages/ui-core/server/roadmap_parse.py"
  - "packages/ui-core/server/handlers.py"
  - "packages/ui-core/server/router.py"
autonomous: true
requirements:
  - "OBS-02"
  - "OBS-03"
must_haves:
  truths:
    - "GET /api/source/roadmap?sourceId= resolves the project root from the registry only"
    - "Response includes structured phases, milestone percent, and empty/parseWarnings when applicable"
  artifacts:
    - path: "packages/ui-core/server/roadmap_parse.py"
      provides: "ROADMAP.md markdown parse + milestone derivation"
  key_links:
    - from: "packages/ui-core/server/router.py"
      to: "GET /api/source/roadmap"
      via: "route registration"
---

<objective>
Add stdlib **roadmap parse** and **`GET /api/source/roadmap?sourceId=`** so the dashboard can load structured phase data and milestone progress for a **local** registered source, aligned with `03-CONTEXT.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-roadmap-visualization/03-CONTEXT.md
@.planning/ROADMAP.md
@packages/ui-core/server/handlers.py
@packages/ui-core/server/registry.py
</context>

<tasks>

<task type="execute">
  <name>Roadmap parse module</name>
  <read_first>
    - `.planning/ROADMAP.md` (shape to parse)
    - `packages/ui-core/server/state_focus.py` (tolerant read pattern)
  </read_first>
  <action>
    Create `roadmap_parse.py` with: read UTF-8 text; extract `## Phases` checklist items (phase number, title, `checked` from `- [x]` / `- [ ]`); for each `### Phase N: title` block capture goal, requirements line, success criteria list when present. Return `phases: list[dict]`, `parseWarnings: list[str]`, `rawLength`. Add `derived_milestone_percent(phases)` from completed vs total top-level phase rows, and `milestone_percent_from_state_file(path)` / helper using existing STATE patterns if available (or regex `percent` in frontmatter from `.planning/STATE.md`). Export `parse_roadmap_markdown(raw: str) -> dict` including `phases`, `parseWarnings`, `rawLength`.
  </action>
  <acceptance_criteria>
    - `python3 -m compileall -q packages/ui-core/server` exits 0
    - `grep -q "def parse_roadmap_markdown" packages/ui-core/server/roadmap_parse.py`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>get_source_roadmap handler</name>
  <read_first>
    - `packages/ui-core/server/handlers.py`
    - `packages/ui-core/server/roadmap_parse.py`
  </read_first>
  <action>
    Add `Handlers.get_source_roadmap`: `sourceId` required; `get_by_id`; 404 if unknown; 400 if local path not dir. Resolve `.planning/ROADMAP.md` then root `ROADMAP.md` fallback. If none: 200 JSON `empty: true`, `phases: []`, `message`, `milestonePercent: 0`, `milestoneSource: "derived"`, `rawLength: 0`. Else read file, run `parse_roadmap_markdown`, set `milestoneSource` to `"state"` when `milestone_percent_from_state_file` returns a value else `"derived"`. JSON keys per `03-CONTEXT.md` (include `milestonePercent`, `milestoneSource`, `parseWarnings`, `rawLength`, `phases`, `empty`).
  </action>
  <acceptance_criteria>
    - `grep -q "get_source_roadmap" packages/ui-core/server/handlers.py`
    - `grep -q "/api/source/roadmap" packages/ui-core/server/router.py`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Register route</name>
  <read_first>
    - `packages/ui-core/server/router.py`
  </read_first>
  <action>
    Add `GET /api/source/roadmap` → `Handlers.get_source_roadmap`.
  </action>
  <acceptance_criteria>
    - Route map contains `/api/source/roadmap` under `GET`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `python3 -m compileall -q packages/ui-core/server`
2. With server + registered source, `curl` `GET /api/source/roadmap?sourceId=...` and confirm JSON shape.
</verification>

<output>
Create `03-01-SUMMARY.md` in this directory after execution.
</output>
