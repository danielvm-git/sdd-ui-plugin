---
phase: "03-roadmap-visualization"
plan: "02"
type: "execute"
wave: 2
depends_on: ["03-01-PLAN.md"]
files_modified:
  - "packages/ui-core/app.js"
  - "packages/ui-core/index.html"
  - "packages/ui-core/style.css"
autonomous: true
requirements:
  - "OBS-02"
  - "OBS-03"
  - "UI-02"
  - "INT-03"
must_haves:
  truths:
    - "Overview and Roadmap views switch in the main shell; Roadmap fetches GET /api/source/roadmap?sourceId="
    - "Summary vs Full mode persisted in sessionStorage; 3s poll when Roadmap is active with visibility pause"
  artifacts:
    - path: "packages/ui-core/app.js"
      provides: "viewTab, fetchRoadmap, grid render, polling"
  key_links:
    - from: "packages/ui-core/app.js"
      to: "GET /api/source/roadmap?sourceId"
      via: "fetchRoadmap and poll"
---

<objective>
Add **Overview | Roadmap** navigation, **milestone** bar, **Summary/Full** toggle, **card grid** for phases, and **polling** for roadmap data, using **createElement** / **textContent** for API-driven strings per `03-CONTEXT.md` and `03-01` API.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-roadmap-visualization/03-CONTEXT.md
@packages/ui-core/app.js
@packages/ui-core/style.css
@packages/ui-core/index.html
</context>

<tasks>

<task type="execute">
  <name>State and fetch</name>
  <read_first>
    - `packages/ui-core/app.js`
  </read_first>
  <action>
    Add `state.viewTab` (`"overview" | "roadmap"`), `state.roadmapData` (loading/error/data by source), `getRoadmapViewMode` / `setRoadmapViewMode` using `sessionStorage` key `sdd-ui-roadmap-view` (`summary` | `full`). Add `async function fetchRoadmap(options)` that GETs `apiUrl("/api/source/roadmap?sourceId=" + encodeURIComponent(sid))` and stores payload; on `!res.ok` set error state. Wire `apiUrl` for all fetches. Clear/reuse one `pollId` in `syncViewPoll`: 3s tick calls `fetchSourceOverview` on overview tab and `fetchRoadmap` (silent) on roadmap tab; respect `document.visibilityState`.
  </action>
  <acceptance_criteria>
    - `grep -q "viewTab" packages/ui-core/app.js`
    - `grep -q "source/roadmap" packages/ui-core/app.js`
    - `grep -q "sdd-ui-roadmap-view" packages/ui-core/app.js` OR `grep -q "roadmap-view" packages/ui-core/app.js`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Header tabs + main switch</name>
  <read_first>
    - `packages/ui-core/index.html`
    - `packages/ui-core/app.js`
  </read_first>
  <action>
    Add a header **view switcher** (Overview / Roadmap) in `index.html` (or create in JS) with `data-view` attributes; in `applyProjectHeader` or `render`, show tabs when a source is selected, hide on empty. `renderMainArea` dispatches: overview → existing dashboard; roadmap → `buildRoadmapView(source, roadmapData)` building DOM with `createElement` only (no raw markdown in `innerHTML`).
  </action>
  <acceptance_criteria>
    - `grep -q "view-tab\|viewTab" packages/ui-core/app.js` or `index.html` contains overview/roadmap labels
  </acceptance_criteria>
</task>

<task type="execute">
  <name>buildRoadmapView</name>
  <read_first>
    - `03-CONTEXT.md` (D-03-04–D-03-09)
  </read_first>
  <action>
    Implement: loading/error/empty; milestone line + `progress-track` bar from `milestonePercent`; **Summary | Full** segmented buttons updating session storage and re-render; **grid** of `article.roadmap-card` with title, goal, requirements (Full adds success criteria); optional `parseWarnings` single paragraph; re-use `btn-secondary`, `text-muted` patterns.
  </action>
  <acceptance_criteria>
    - `grep -q "roadmap" packages/ui-core/style.css`
    - `grep -q "buildRoadmapView\|roadmap-grid" packages/ui-core/app.js` OR `roadmap`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Style polish</name>
  <read_first>
    - `packages/ui-core/style.css`
  </read_first>
  <action>
    Add BEM-style rules for `.roadmap-view`, `.roadmap-grid`, `.roadmap-card`, `.roadmap-milestone` / `.progress-track` as needed; use existing tokens (`--border`, `--card-bg`); 16/24px spacing.
  </action>
  <acceptance_criteria>
    - `grep -q ".roadmap" packages/ui-core/style.css`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Manual: add source, switch to Roadmap, see cards; toggle Summary/Full; confirm network 3s on roadmap; hide tab → polling stops.
2. `rg "innerHTML" packages/ui-core/app.js` — roadmap must not set unsanitized API strings into innerHTML.
</verification>

<output>
Create `03-02-SUMMARY.md` in this directory (overwrite orphan stub when done).
</output>
