---
phase: "01-foundation-registry"
plan: "03"
type: "execute"
wave: 3
depends_on: ["01-02-PLAN.md"]
files_modified:
  - "packages/ui-core/app.js"
  - "packages/cli/src/index.js"
  - "packages/cli/bin/sdd-ui.js"
autonomous: true
requirements:
  - "SRC-01"
must_haves:
  truths:
    - "User can add a local directory via the UI"
    - "New source appears in the sidebar immediately"
    - "Sources persist after server restart"
    - "sdd-ui serve starts the unified dashboard"
  artifacts:
    - path: "packages/cli/src/index.js"
      provides: "New CLI command"
  key_links:
    - from: "packages/cli/src/index.js"
      to: "packages/ui-core/app.py"
      via: "spawnDetached"
---

<objective>
Enable project source management and integrate with the CLI.

Purpose: Complete the loop by allowing users to register local projects and launch the unified dashboard via the terminal.
Output: A fully functional source management system and a new CLI command.
</objective>

<execution_context>
@/Users/me/Sites/sdd-ui-plugin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/me/Sites/sdd-ui-plugin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-registry/01-CONTEXT.md
@.planning/phases/01-foundation-registry/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Source Addition Logic</name>
  <files>
    - packages/ui-core/app.js
    - packages/ui-core/server/handlers.py
  </files>
  <action>
    Implement the "Add Project Source" functionality per D-03, D-04, and D-05.
    - In app.js: Implement Hybrid Approach. 
      1. Try `window.showDirectoryPicker()` if available.
      2. Fallback to a modal/prompt for manual path input.
    - POST /api/registry: Validate the path exists (in handlers.py) and use RegistryManager to save.
    - Refresh sourcesList and update the sidebar upon successful addition.
  </action>
  <verify>
    <automated>curl -X POST -d '{"path": "/tmp", "name": "Test"}' http://localhost:3000/api/registry && curl http://localhost:3000/api/registry | grep "Test"</automated>
  </verify>
  <done>User can add a project via UI; it persists in registry.json.</done>
</task>

<task type="auto">
  <name>Task 2: CLI Integration</name>
  <files>
    - packages/cli/src/index.js
  </files>
  <action>
    Integrate the unified dashboard into the CLI.
    - Add `serve` command to `COMMANDS` in `packages/cli/src/index.js`.
    - `runServe` should:
      1. Determine an available port (default 3000).
      2. Spawn `python3 packages/ui-core/app.py` in the background.
      3. Open the browser to the local UI.
    - Update `runHelp` to include the `serve` command.
  </action>
  <verify>
    <automated>node packages/cli/bin/sdd-ui.js serve --dry-run</automated>
  </verify>
  <done>CLI supports `sdd-ui serve` to launch the unified dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Local FS | Picking directories via browser API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Elevation of Privilege | Directory Picker | mitigate | Ensure only directory handles are stored, and the server validates path accessibility. |
</threat_model>

<verification>
Run `node packages/cli/bin/sdd-ui.js serve`. In the browser, add a local directory. Refresh and verify it remains.
</verification>

<success_criteria>
- User can successfully add a local project.
- Added project is visible in the sidebar.
- Registry.json is updated on disk.
- CLI command `sdd-ui serve` works.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-registry/01-03-SUMMARY.md
</output>
