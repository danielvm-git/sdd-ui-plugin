---
phase: "01-foundation-registry"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "packages/ui-core/app.py"
  - "packages/ui-core/server/registry.py"
  - "packages/ui-core/server/router.py"
  - "packages/ui-core/server/handlers.py"
autonomous: true
requirements:
  - "CORE-01"
  - "SRC-05"
must_haves:
  truths:
    - "Python server starts and responds to /api/health"
    - "Registry file is created at ~/.sdd-ui/registry.json if missing"
    - "API returns the list of registered projects"
  artifacts:
    - path: "packages/ui-core/app.py"
      provides: "Server entry point"
    - path: "packages/ui-core/server/registry.py"
      provides: "Registry persistence logic"
    - path: "packages/ui-core/server/router.py"
      provides: "Request routing"
  key_links:
    - from: "packages/ui-core/app.py"
      to: "packages/ui-core/server/router.py"
      via: "import and handler assignment"
---

<objective>
Establish the modular Python backend and the persistent registry system for SDD project sources.

Purpose: Provide a stable, zero-dependency foundation for the unified dashboard and cross-instance persistence.
Output: A running Python server with an API for registry management.
</objective>

<execution_context>
@/Users/me/Sites/sdd-ui-plugin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/me/Sites/sdd-ui-plugin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-registry/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Modular Server Structure</name>
  <files>
    - packages/ui-core/app.py
    - packages/ui-core/server/router.py
    - packages/ui-core/server/handlers.py
  </files>
  <action>
    Create a modular Python http.server implementation per D-06 and D-07.
    - app.py: Entry point, configures PORT (default 3000) and launches server.
    - router.py: Maps URL paths to handler functions.
    - handlers.py: Implements GET /api/health and GET /api/registry endpoints.
    - Use standard library only (http.server, json, os, sys).
    - Ensure CORS headers are included for local development.
  </action>
  <verify>
    <automated>python3 packages/ui-core/app.py & (sleep 2 && curl -s http://localhost:3000/api/health && kill $!)</automated>
  </verify>
  <done>Server responds with JSON status "ok" on /api/health.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Persistent Registry Manager</name>
  <files>
    - packages/ui-core/server/registry.py
  </files>
  <action>
    Implement registry persistence per D-01 and D-02.
    - Path: ~/.sdd-ui/registry.json
    - Logic: Load from file (return [] if missing), Save to file (ensure dir exists).
    - Registry structure: List of objects { id: string, name: string, path: string, type: 'gsd'|'bmad'|'spec-kit'|'unknown', added_at: timestamp }.
    - Add RegistryManager to handlers.py to support GET /api/registry and POST /api/registry (stub for now).
  </action>
  <verify>
    <automated>python3 -c "from packages.ui_core.server.registry import RegistryManager; rm = RegistryManager(); print(rm.get_all())"</automated>
  </verify>
  <done>RegistryManager correctly reads/writes to ~/.sdd-ui/registry.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Python API | Untrusted JSON data and path strings cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | registry.json | mitigate | Ensure registry file has restricted permissions (600) |
| T-01-01-02 | Tampering | /api/registry POST | mitigate | Validate that incoming paths are absolute and exist on disk |
</threat_model>

<verification>
Start the server and use curl to verify the registry API returns an empty list (or existing list if present).
</verification>

<success_criteria>
- Python server starts on configured port.
- API endpoints /api/health and /api/registry work.
- Registry file is automatically initialized in ~/.sdd-ui/.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-registry/01-01-SUMMARY.md
</output>
