---
phase: "01-foundation-registry"
plan: "02"
type: "execute"
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified:
  - "packages/ui-core/index.html"
  - "packages/ui-core/style.css"
  - "packages/ui-core/app.js"
autonomous: true
requirements:
  - "CORE-02"
must_haves:
  truths:
    - "Sidebar is visible and 240px wide"
    - "Empty state is displayed when no projects are registered"
    - "Onboarding tour triggers for new users"
  artifacts:
    - path: "packages/ui-core/index.html"
      provides: "Main entry point"
    - path: "packages/ui-core/app.js"
      provides: "Frontend logic"
  key_links:
    - from: "packages/ui-core/app.js"
      to: "/api/registry"
      via: "fetch"
---

<objective>
Establish the Core UI shell and initial onboarding experience.

Purpose: Provide a professional, interactive dashboard following the UI-SPEC and onboarding users to the SDD ecosystem.
Output: A functional frontend with sidebar, empty state, and onboarding tour.
</objective>

<execution_context>
@/Users/me/Sites/sdd-ui-plugin/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/me/Sites/sdd-ui-plugin/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-registry/01-UI-SPEC.md
@.planning/phases/01-foundation-registry/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold UI Shell</name>
  <files>
    - packages/ui-core/index.html
    - packages/ui-core/style.css
    - packages/ui-core/app.js
  </files>
  <action>
    Implement the main layout per UI-SPEC.
    - index.html: Define Sidebar (240px) and Main Content area. Include Lucide icons via CDN.
    - style.css: Implement color palette (Dark theme #09090b, #18181b), typography, and spacing scale.
    - app.js: Initial state management (currentSource, sourcesList).
    - Ensure app.py serves these files as static assets.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000 | grep "sidebar"</automated>
  </verify>
  <done>UI layout matches UI-SPEC with sidebar and main content area.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Empty State & Onboarding</name>
  <files>
    - packages/ui-core/app.js
    - packages/ui-core/style.css
  </files>
  <action>
    Implement the onboarding flow per D-08 and D-09.
    - If sourcesList is empty, display the "No Projects Registered" empty state in the main area.
    - Implement a simple "Onboarding Tour" overlay or tooltip that guides the user to the "Add Project Source" button.
    - Add a "Primary CTA" button as defined in UI-SPEC.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000 | grep "No Projects Registered"</automated>
  </verify>
  <done>Empty state is visible when no projects exist; onboarding tour triggers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CDN → Browser | Loading Lucide icons from third-party CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | External Scripts | mitigate | Use Subresource Integrity (SRI) for Lucide CDN script/style |
</threat_model>

<verification>
Start the server and visit http://localhost:3000. Verify the sidebar is present and the empty state is shown.
</verification>

<success_criteria>
- UI renders correctly in browser.
- Dark theme applied according to UI-SPEC.
- Lucide icons load correctly.
- Empty state and onboarding tour logic works.
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-registry/01-02-SUMMARY.md
</output>
